import React from "react";
import { StyleSheet, View, Text, Image, TouchableOpacity } from "react-native";

export default function FilterBox({ props }) {
  console.log('what are these props', props.taxon)


  return (
    <View>
      <TouchableOpacity>

      </TouchableOpacity>
    </View>
  )
}
